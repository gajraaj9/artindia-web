{
  "name": "artindia-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test 'test/*.test.mjs'"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
